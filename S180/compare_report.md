# Comparison Report

This document summarizes pre-fix (flaky) vs post-fix (deterministic) results. Run `bash run_all.sh` to reproduce.

Summary metrics and per-case details are generated by the run_all.sh test harness and saved in `results/` and `compare_report.md`.
