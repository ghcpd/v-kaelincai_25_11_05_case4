# Project_B_Deterministic

This project demonstrates a deterministic grading service.

Setup:
- Run `bash setup.sh` to create venv and install dependencies.

Run tests:
- `bash run_tests.sh` or `.\run_tests.ps1` in PowerShell. Results are written to `../results/results_post.json`.
